# PuCheckbox Reset Removal Impact

## Existing Source

Web package source to remove:

```text
packages/web/src/components/puCheckbox/puCheckbox.vue
packages/web/src/components/puCheckbox/puCheckbox.ts
packages/web/src/components/puCheckbox/puCheckbox.scss
packages/web/src/components/puCheckboxGroup/puCheckboxGroup.vue
packages/web/src/components/puCheckboxGroup/puCheckboxGroup.ts
packages/web/src/components/puCheckboxGroup/puCheckboxGroup.scss
```

UniApp package source is out of scope for this task:

```text
Do not edit packages/uniapp.
```

Web stories to remove:

```text
packages/web/src/stories/forms/PuCheckbox.story.vue
packages/web/src/stories/forms/PuCheckboxGroup.story.vue
```

## Public And Generated Surfaces

Web generated registry surfaces:

```text
packages/web/src/component-registry.ts
packages/web/types/components.d.ts
```

These files are generated by:

```text
pnpm --filter @partner-up-dev/design-web run generate
```

After the component directories are deleted, generation should remove:

```text
PuCheckbox
PuCheckboxGroup
export type * from './components/puCheckbox/puCheckbox'
export type * from './components/puCheckboxGroup/puCheckboxGroup'
global component declarations for PuCheckbox and PuCheckboxGroup
installer registry entries for PuCheckbox and PuCheckboxGroup
```

Web Histoire configuration to update manually:

```text
packages/web/histoire.config.ts
```

Remove tree mappings for:

```text
PuCheckbox
PuCheckboxGroup
```

Web skill seed to update manually:

```text
packages/web/skill.seed.json
packages/web/scripts/generate-agent-skill.mjs
```

Remove component seed entries for:

```text
PuCheckbox
PuCheckboxGroup
```

Also remove or replace the explicit `PuCheckbox` usage in the generated
composition recipe source. The current `Form Page` recipe includes PuCheckbox
directly.

Then regenerate or check generated skill references with:

```text
pnpm --filter @partner-up-dev/design-web run skill:generate
```

Expected generated web skill removals include:

```text
packages/web/skills/design-web/references/components/PuCheckbox.md
packages/web/skills/design-web/references/components/PuCheckboxGroup.md
packages/web/skills/design-web/references/component-map.md references
composition and usage references that mention the deleted components
```

## Documentation And Migration

Breaking public API removal should update:

```text
packages/web/MIGRATION.md
```

Record that the current checkbox components were removed because their contract
was rejected, and that consumers should not migrate to the old API. If no
replacement exists yet, say that explicitly.

Likely docs references to audit after removal:

```text
packages/web/README.md
packages/web/docs/shape-audit.md
packages/web/skills/design-web/references/components/PuToggleSwitch.md
packages/web/skills/design-web/references/components/PuForm.md
packages/web/skills/design-web/references/components/PuFormItem.md
packages/web/skills/design-web/references/composition-recipes.md
tasks/web-form-capabilities/*
tasks/frontend-primitive-coverage-gap/*
tasks/content-presentation-components/*
```

Do not rewrite historical task packets unless they create current verification
failures. Prefer recording the new reset in this packet and migration guides.

## Verification Gates

Recommended checks after removal:

```text
pnpm --filter @partner-up-dev/design-web run generate
pnpm --filter @partner-up-dev/design-web run skill:generate
pnpm --filter @partner-up-dev/design-web run type-check
pnpm --filter @partner-up-dev/design-web run story:coverage
```

Run broader verification if time allows:

```text
pnpm --filter @partner-up-dev/design-web run verify
```

## Known Risk

This is a public component removal for the web package. It is release-worthy
and breaking for consumers that import, globally use, or rely on generated
global component typing for web PuCheckbox or PuCheckboxGroup.
