---
"@partner-up-dev/design-web": patch
---

Add `PuChipInput` for editing a single chip value, add `PuChipsEditor` for editable chip collections, use primary active borders for neutral chip editing fields, and default `PuChip` geometry to rectangular chips.
