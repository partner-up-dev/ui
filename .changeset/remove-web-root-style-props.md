---
"@partner-up-dev/design-web": patch
---

Remove UniApp-style `customClass` and `customStyle` root styling props from web components; use standard Vue `class` and `style` fallthrough on component roots instead.
